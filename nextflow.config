docker.enabled = true
process.container = 'iprtk/nflow-test:latest'
profiles {
    awsbatch {
        process.executor = 'awsbatch'
        process.queue = 'TEST-nextflow-batch-queue-ec2'
        aws.region = 'eu-west-1'
    }
    local {
        process.executor = 'local'
    }
}
